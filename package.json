{
  "name": "616byron-profile-assets",
  "version": "1.0.0",
  "private": true,
  "description": "Optional local renderer for the LB / 616 profile artwork.",
  "scripts": {
    "build:workstation": "node scripts/build-workstation.cjs",
    "render:assets": "node scripts/render-assets.cjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "2.6.2"
  }
}
